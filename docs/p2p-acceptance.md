# Проверки подписанного обмена — 2026-09-15

Промежуточные результаты текущей реализации dev-схемы 4 на macOS.
Этот отчёт не закрывает весь этап и проверки безопасности из roadmap.
Все записи и пароли в сценариях искусственные, с маркером `PUBLIC`.

## Проверено

- `sh scripts/check.sh`: полный прогон завершился успешно — архитектура,
  Rustfmt, сборка targets, Clippy, тесты, Rustdoc, release-сборка и headless smoke.
  Затронутые схемы экспортированы и визуально просмотрены.

- `python3 scripts/test-cli-p2p.py`: три независимых процесса CLI, три профиля
  с настоящим macOS Keychain, приглашение по Iroh, явный допуск управляющим,
  загрузка нового файла, разблокировка и сходимость подписанной правки.
  Заблокированный получатель сохраняет ciphertext и применяет его при входе.
- `cargo test -p taypeer-cli --test p2p`: тот же сценарий через локальный HTTPS
  relay. В каждом CLI отключены прямые IP-транспорты; тестовый сертификат явно
  передаётся как доверенный корень. Проверка сертификата и имени сервера включена.
- `cargo test -p taypeer-services managed::tests`: пять сценариев: долговечные
  правки и копия только для чтения; офлайн-конфликт/история/черновик через ротацию;
  точный повтор административного действия после открытия; отказ старым токенам
  после повторной аутентификации; отказ непринятым источникам отозванного автора
  и их зависимостям внутри checkpoint действующего участника. Независимая правка
  действующего автора в последнем сценарии применяется.

Нативные тесты требуют доступа к Keychain и локальным сокетам. В песочнице
отказ credentials ожидаем; он не подменяется файловым хранением ключей.
Не пересобирать CLI во время нативного прогона: Keychain связывает доступ
с исполняемым приложением. Временные файлы тестов удаляются; созданные изолированные
тестовые записи Keychain пока остаются в системном хранилище.

## Воспроизведение

```sh
cargo build -p taypeer-cli
python3 scripts/test-cli-p2p.py
cargo test -p taypeer-cli --test p2p
cargo test -p taypeer-services managed::tests
```

В локальной сессии владельца перед командами использовать `rtk`; Python — через
`rtk proxy python3`. Интерактивный запуск с отдельным профилем:

```sh
target/debug/taypeer-cli --profile /tmp/taypeer-public-a session
```

Внутри сессии: `db create`, `sync start`, `invite create`. На втором профиле
(также на другом Mac): `invite join /путь/к/новой.taypeer`, ввести код в скрытый
prompt. Управляющий выполняет `invite requests`, затем `invite approve <id>`;
получатель — `invite resume <id>` и `db unlock`. Для внешней сети можно запустить
`sync start --relay default` до создания приглашения. Прямой маршрут требует
взаимной сетевой достижимости. Пароль передаётся отдельно от приглашения.

## Открыто

- UI-проверка macOS отложена по указанию владельца; в текущую проверку готовности
  её не включаем.
- Восстановление в новый доверенный набор, ручной разбор непринятых сетевых
  источников и безопасная очистка их ciphertext с учётом всех удержаний.
- Полная матрица handoff/отказов хранения/расхождений управления, длительный fuzzing,
  независимый аудит, Android, KDBX и защита полного отката ОС.

Контракт желаемого этапа находится в [p2p.md](p2p.md). Наличие описания в контракте
не означает, что все перечисленные возможности уже реализованы и проверены.
