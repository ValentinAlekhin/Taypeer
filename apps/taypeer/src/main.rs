//! Development bootstrap; the GPUI application will replace this entry point.

fn main() {
    println!(
        "Taypeer {} — repository scaffold.\n\
         No UI or vault operations are implemented. See docs/roadmap.md.",
        env!("CARGO_PKG_VERSION")
    );
}
