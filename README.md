# Taypeer

Локальный менеджер паролей одного человека для macOS 15+ Apple Silicon
и Android 12+ ARM64. [Продуктовые требования](spec.md).

Название продукта — **Taypeer**, расширение БД — `.taypeer`.
Корневая папка локального проекта и папка `wireframes/` не переименованы.
Исторические отчёты и доказательства в `spikes/` сохраняют прежнее имя;
байтовые метки исследовательского криптопротокола не меняются при ребрендинге.

**Стадия:** первый Rust-срез и macOS demo на искусственных данных в памяти:
группы, записи, адресные изменения Automerge, история и сессии.
[Запуск и ограничения demo](docs/demo.md).
[Решение по исследованиям](spikes/README.md) разрешает этот этап;
[незакрытые проверки](docs/roadmap.md#2-локальная-бд-и-первая-вертикаль-интерфейса)
остаются обязательными до настоящих паролей.

## Начать разработку

Нужны Rust через rustup и pnpm **10.17.1**. `rust-toolchain.toml` выбирает
проверенный в macOS-стенде Rust **1.97.1** с rustfmt и Clippy.
pnpm устанавливает проектный Node.js **22.22.3** по lockfile.

```sh
pnpm install --frozen-lockfile
pnpm arch:dev
cargo run --locked -p taypeer -- --demo
sh scripts/check.sh
```

На машине владельца добавляй `rtk` перед командами. Перед каждой задачей сначала
проверь архитектурное влияние; порядок описан в [AGENTS.md](AGENTS.md).
Для отдельных проверок Rust доступны `cargo check-all`, `cargo lint`,
`cargo test-all` и `cargo fmt --all -- --check`.
После загрузки закреплённых зависимостей проверки можно запускать без сети.
Явный офлайн-режим Rust:
`CARGO_NET_OFFLINE=true sh scripts/check.sh`.

Общий скрипт проверяет LikeC4, локальные ссылки, Rustdoc, сборку, поведенческие
тесты Rust и конечный `--smoke-test` без открытия окна.
[CI](.github/workflows/ci.yml) выполняет проверки на macOS и Ubuntu и сохраняет
сборку архитектуры как артефакт. Ubuntu проверяет переносимость ядра;
Linux не является целевой платформой продукта. Результат удалённого CI появится
после публикации репозитория и первого запуска workflow.

## Навигация

| Источник | Назначение |
| --- | --- |
| [Архитектура](docs/architecture.md) | Просмотр LikeC4, карта представлений, принципы и владение документацией |
| [Скилл Rust](.agents/skills/taypeer-rust/SKILL.md) | Обязательные правила работы, декомпозиция, идиомы Rust и критерии ревью |
| [Спецификация](spec.md), [словарь](CONTEXT.md) | Требования и термины |
| [Данные](docs/data-model.md), [совместимость](docs/compatibility.md) | Точные инженерные контракты |
| [Первый Rust-срез и macOS demo](docs/demo.md) | Запуск, временные данные и границы реализации |
| [Roadmap](docs/roadmap.md), [проверки](docs/testing.md) | Следующие работы и способы приёмки |
| [Исследования](spikes/README.md), [референсы](docs/references.md) | Решения, доказательства и ревизии |
| [Wireframes](wireframes/README.md) | Макеты и правила композиции UI |
| [Fixtures](tests/fixtures/README.md) | Искусственные тестовые данные |

`ref/` исключён из Git и не нужен для сборки. Временные БД и ключи хранятся
в исключённом из Git `local-data/`; результаты генерации архитектуры — в `artifacts/`.
Лицензия собственного кода пока не выбрана; сторонние лицензии и атрибуция сохраняются.

## Командный клиент

`cargo run --locked -p taypeer-cli -- session` запускает интерактивную работу
с зашифрованными файлами. [Команды, пример и текущие ограничения](docs/core-cli.md).
